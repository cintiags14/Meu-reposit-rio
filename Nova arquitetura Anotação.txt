
***
Local host: 127.0.0.1
Máquina do israel: 10.50.0.3

Atualmente:
username banco: postgres
senha: root

Porta padrão do postgre: 5432

entra no banco que foi criado > schemas > public > (tabela )

localhost:8080 [...] e 127.0.0.1.:8080 (tem a mesma função de acesso)


atualizar database e depois dropar/delete o banco

ftt.unievangelica.edu.br/gitlab

CREATE EXTENSION unaccent; > "trás os dados"
***

O que CREATE EXTENSION unaccent(banco de dados) faz?

CREATE EXTENSION carrega uma nova extensão no banco de dados atual.
Carregar uma extensão basicamente equivale a executar o arquivo de script da extensão. 
 além disso, registra as identidades de todos os objetos criados, para que eles possam ser descartados novamente se DROP EXTENSION for emitido.

front e back serão comandos diferentees para executar?

com a nova arquitetura, será executado na máquina local ou no servidor? 

como executar um projeto com angular (versão 4 e superior)?

como executar um projeto com spring boot em MaVeN?

como deletar dados das tabelas (banco de dados)?

como manipular o banco de dados?

como o projeto conecta com o banco?




